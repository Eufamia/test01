1.使用输出流来拷贝时。大小会变化，同时拷贝之后的文件无法打开
不知道怎么解决
2.在测试file的常用方法时，出现了目录无法创建的问题（已解决）
解决方法：原因是测试时是创建文件和创建目录先后测试，同时使用的时一个名字
由于exists();方法无法判断这个名字对应的是文件还是目录，所以判断时，只要相同位置已经存在了一个此名称，
后面的代码就无法创建目录了。
3.我自己在编写的时候，也就是修改了类的属性，然后反序列化并没有报错，不知道为什么
因为序列号和反序列化在同一个文件中，所有即使改动并且自动生成了序列号，也是
可以自动编译成功的，但是实际开发中可能是分开写的，所以还是建议使用自定的序列号版本号
这是唯一区分一个类的

4.使用interrupt来唤醒线程时，出现InterruptedException，不知道哪里出错，没有走try分支，直接打印错误信息了，暂时无法解决
文件名InterruptTest.java
5.行级锁：在一条sql语句后面加上for update 可以锁定数据，这种情况下在事务没有结束之前，其他人无法对这些数据修改；
6.关于servlet的错误，设置字符集最好是放在方法的开头，否则可能导致后续数据失效，仍然按照之前之前的字符集解码
7.关于更新数据的错误，目前还没有很好的解决方法，我是将update分开在两个程序进行写入，这样确保了更新的成功
8.关于cookie的联系，如果第一次使用一个浏览器可以正常获取使用cookie，如果返回vip页面重新消费就是不成功的，
之前有过尝试是可以成功的，有点奇怪，并且如果第一次使用过了，第二次再次重新注册，仍然是有错的，或者是数字格式化异常或者是空指针异常
根本原因是无法获取cookie和保存cookie，这种情况就跟老师所讲cookie生命周期不一样。暂无解

9.今天遇到的一个错，其实老师之前有讲过，之前一直没注意到，是因为之前是严格按照老师的顺序走的，这次是自己写，所以出现了问题
也就是一个web项目里面必须要把自己使用的jar包添加到lib下面，因为项目加载到浏览器跟运行在idea是不一样的，如果是运行在idea，直接导入到库就行了
错误点：no sutiable Driver
10.关于项目部署的问题：如果在编辑一个项目到一半打开别的项目，关闭前一个项目，再打开就找不到了，这个时idea设置的问题，可以重新部署，重点时project structure里面的
最好时从头开始走一遍，检查是否有问题。






















