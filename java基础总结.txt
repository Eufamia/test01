1.java语言特性
	简单性
	面向对象
	多线程
	健壮性
	可移植性（跨平台性）
2.JDK,JRE和JVM之间的关系
3.标识符命名规则和规范
      规则


	只能由数字字母下划线以及美元符号组成
	不能以数字开头
	java中的关键字和保留字不能作为标识符
	标识符严格区分大小写
	理论上是没有长度限制的
     规范
 	类名和接口名都是首字母大写，并且如果
	由多个单词组成，每个单词首字母大写
    
	变量名方法名都是首字母小写第二个单词开始
	每个单词首字母大写，遵循驼峰命名方式，同时
	常量全部使用大写字母，单词与单词之间以下划线划分
4.基本数据类型8种
	变量在声明之后一定要初始化，否则会报错
5.字符编码
	ASCII码 在 ASCII 码中规定'a'对应 97，'b'对应 98，以此类推，
    'A'对应 65，'B'对应 66，以此类推，'0'字符对应 48，'1'字符对应 49

	国标码ISO-8859-1 不支持中文
	支持简体中文的字符集GB2312<GBK<GB18030(容量)
	支持繁体中文的是Big5
	Unicode支持所有国家的文字，主要由UTF-8,UTF-16,UTF-32
6.基本数据类型转换规则
	八种基本数据类型中，除 boolean 类型不能转换，剩下七种类型之间都可以进行转换；
	 如果整数型字面量没有超出 byte,short,char 的取值范围，可以直接将其赋值给
     byte,short,char 类型的变量；
	小容量向大容量转换称为自动类型转换，容量从小到大的排序为：byte < short(char) <
      int < long < float < double，其中 short 和 char 都占用两个字节，但是 char 可以表示更大
      的正整数；
	大容量转换成小容量，称为强制类型转换，编写时必须添加“强制类型转换符”，但
      运行时可能出现精度损失，谨慎使用；
	byte,short,char 类型混合运算时，先各自转换成 int 类型再做运算；
	多种数据类型混合运算，各自先转换成容量最大的那一种再做运算；